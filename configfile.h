#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <sstream>


void config(std::string &normal_plus,
            std::string &normal_tilde,
            std::string &meme_plus,
            std::string &meme_tilde,
            std::string &devicename);
